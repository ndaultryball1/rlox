Playing around with creating a language. Current features:

* Tokenise simple arithmetic expressions.
* Evaluate simple arithmetic expressions.

But haven't done the parser yet so these are not hooked together...